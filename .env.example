OPENAI_API_KEY=your_api_key_here
OPENAI_MODEL=gpt-4.1-mini

# Local/dev fallback: omit these and answers are stored in data/answers.json.
# GCP Cloud Run + Cloud SQL:
INSTANCE_CONNECTION_NAME=your-project:us-central1:first-question-db
DB_NAME=first_question
DB_USER=first_question_user
DB_PASSWORD=replace_me
